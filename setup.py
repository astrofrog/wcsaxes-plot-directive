#!/usr/bin/env python

from distutils.core import setup

setup(name='plot_directive',
      version='0.0.0',
      packages=['plot_directive'],
      provides=['plot_directive'],
      requires=['matplotlib'],
      )
